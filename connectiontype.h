#ifndef CONNECTIONTYPE_H
#define CONNECTIONTYPE_H

// I realize that it would be cool to use generics instead
// but C++ generics are really stupid, you end up writing more duplicate code than you would otherwise
enum ConnectionType {Bit, Byte};

#endif // CONNECTIONTYPE_H
